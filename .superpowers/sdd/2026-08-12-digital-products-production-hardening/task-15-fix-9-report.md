# Task 15 fix round 9

- Signing-failure evidence compares exact grant IDs immediately before and after the real fault; grace evidence proves the same grant ID and count; sixth denial records its real HTTP status and production UI copy.
- Download integrity follows the application redirect chain and hashes only the final storage HTTP 200 bytes after checking `Content-Disposition`.
- Standalone verification rejects duplicate scenarios and cross-checks grant/session/version, retained replacement manifests, and ordered failed-to-succeeded delivery history.
- Evidence scanning also rejects Bearer authorization and recognizable provider-secret material.
- Accessibility workflows now keyboard-operate upload/publish and checkout, and exercise replacement dialog trapping, reverse traversal, Escape dismissal, and focus return.

External provider success remains unclaimed until the nonproduction gate is executed with its configured providers.
