{
  "framework": "nextjs",
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ]; then echo \"Allowing deployment for main\"; exit 1; elif [ -n \"$VERCEL_GIT_COMMIT_REF\" ]; then echo \"Skipping Git-triggered deployment for non-main ref: $VERCEL_GIT_COMMIT_REF\"; exit 0; else exit 1; fi"
}
